---
import EntregarTarea from "../../../../components/EntregarTarea.astro";
import Layout from "../../../../layouts/Layout.astro";
import { db, Tareas } from "astro:db";
const tareaInfo = await db.select().from(Tareas);
const { clase, tarea } = Astro.params;
---

<Layout title={tarea ? tarea : ""}>
  {
    tareaInfo.map(({ name }: any) => {
      console.log(name);
    })
  }
</Layout>
